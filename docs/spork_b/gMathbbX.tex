\magicSubsection{$\mathbb{X}$ (mathbb-x)}{sec:gMathbbX}

Set of data variable names.
These are variables that are not of control ($\mathbb{Y}$, def~\ref{sec:gMathbbY}) or barrier ($\mathbb{B}$, def~\ref{sec:gMathbbB}) type.

% >Y

