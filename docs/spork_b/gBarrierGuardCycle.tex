\magicSubsection{Barrier Guard Cycle}{sec:gBarrierGuardCycle}

The barrier guard cycle for a given barrier name $z$ is the set of barrier names in $\{ \textsf{guardedBy}^n(z)\mid n = 1, 2, 3... \}$ (def~\ref{sec:gGuardedBy}).

